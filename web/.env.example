# Base URL of the NestJS API.
#
# Leave this EMPTY for local development — the Vite dev server proxies /api
# straight to http://localhost:3000 (see vite.config.ts).
#
# Set it when the API is deployed somewhere else, e.g.
# VITE_API_URL=https://omid-portfolio-api.onrender.com
VITE_API_URL=
