{
  "name": "omid-portfolio",
  "version": "1.0.0",
  "description": "Portfolio site for Omid Deldar — React front end, NestJS API, interactive terminal",
  "author": "Omid Deldar",
  "license": "MIT",
  "private": true,
  "scripts": {
    "install:all": "npm install --prefix api && npm install --prefix web",
    "sync": "node scripts/sync-shared.mjs",
    "dev": "concurrently -n api,web -c magenta,cyan \"npm:dev:api\" \"npm:dev:web\"",
    "dev:api": "npm run dev --prefix api",
    "dev:web": "npm run dev --prefix web",
    "build": "npm run sync && npm run build --prefix api && npm run build --prefix web",
    "start": "npm run start:prod --prefix api",
    "typecheck": "npm run typecheck --prefix api && npm run typecheck --prefix web",
    "preview": "npm run preview --prefix web",
    "docker": "docker compose up --build",
    "docker:down": "docker compose down",
    "docker:logs": "docker compose logs -f"
  },
  "devDependencies": {
    "concurrently": "^9.1.2"
  }
}
